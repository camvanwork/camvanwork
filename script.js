const menuToggle=document.querySelector('.menu-toggle');const navLinks=document.querySelector('.nav-links');if(menuToggle&&navLinks){menuToggle.addEventListener('click',()=>{const open=navLinks.classList.toggle('open');menuToggle.setAttribute('aria-expanded',String(open));menuToggle.setAttribute('aria-label',open?'Đóng menu':'Mở menu')});navLinks.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{navLinks.classList.remove('open');menuToggle.setAttribute('aria-expanded','false')}))}
const caseLinks=['work/goffice-seo/','work/elipsport-seo/','work/facebook-ads/'];document.querySelectorAll('.case-card').forEach((card,i)=>{if(!caseLinks[i]||card.querySelector('.case-detail-link'))return;const link=document.createElement('a');link.className='arrow-link case-detail-link';link.href=caseLinks[i];link.textContent='Read case study ↗';link.style.display='inline-block';link.style.marginTop='26px';const target=card.querySelector('.case-body>div:first-child')||card;target.appendChild(link)});